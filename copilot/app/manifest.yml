# The manifest for the "app" service.
# Read the full specification for the "Load Balanced Web Service" type at:
#  https://aws.github.io/copilot-cli/docs/manifest/lb-web-service/

name: room-matching
type: Load Balanced Web Service

http:
  redirect_to_https: false
  path: '/'

image:
  build: Dockerfile
  port: 8080

cpu: 4096
memory: 8192
count: 1
exec: true
network:
  connect: true

environments:
  staging:
    http:
      alb: whitel-Publi-PdbUyJzG57CQ
      alias: staging-room-matching.wlbl.cloud
  production:
    http:
      alb: white-Publi-YO6OGAEXCY80
      alias: room-matching.wlbl.cloud